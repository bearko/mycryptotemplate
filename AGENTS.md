# AI Agent 運用ガイド

このリポジトリで動く AI エージェント (= Claude Code セッション、 サブエージェント、
スキル経由の実行) はこの規約に従って動作します。

## 必読

CLAUDE.md → docs/charters/ → 該当 patterns。 順番厳守。

## エージェント種別と用途

| 種別 | 用途 | 起動 |
|---|---|---|
| **メインセッション** | 機能実装、 SPEC 執筆、 PR 作成 | `claude` コマンド |
| **Explore (= サブ)** | コードベース調査、 開発前の audit | `Agent({ subagent_type: "Explore" })` |
| **Plan** | 大規模実装の設計、 アーキテクチャ判断 | `Agent({ subagent_type: "Plan" })` |
| **general-purpose** | 並列調査、 多段検索 | `Agent({ subagent_type: "general-purpose" })` |
| **claude-code-guide** | Claude Code 自体の使い方Q&A | `Agent({ subagent_type: "claude-code-guide" })` |

### サブエージェントを使うべき場面

- **コード探索が複雑** (= 「この機能はどこで実装されているか」 を多ファイル横断で調べる) → Explore
- **大規模 refactor の事前設計** (= 影響範囲洗い出し + 段階分割) → Plan
- **並列で 2 つの調査** (= 双子プロジェクトの差分など) → 2 つの Agent を 1 メッセージで起動
- **トリビアル / 1 ファイル確認** → サブエージェント不要、 メインで Read/Grep

### サブエージェントに渡すプロンプトの作法

- **背景**: なぜ調査が必要か、 何を作っているか
- **既知**: 既に分かっていること、 試したこと
- **観点**: 調査対象の項目を箇条書き
- **粒度**: 「短く」「詳細に」 を明示。 デフォルトは長文 OK
- **出力形式**: 「マークダウン表で」「ファイルパス + 行番号併記で」 等を指定

## Human-in-the-Loop (HITL) のタイミング

以下は **必ずユーザーに確認** すること:

- 仕様 (= SPEC) と矛盾する実装要求が来たとき
- 破壊的変更 (= データロスを起こしうる変更、 git --force 系)
- セキュリティ・法令・倫理に関わる判断
- 外部 API キー / 認証情報の取り扱い
- 大きな refactor (= 100+ 箇所同時変更)
- ライブラリ追加 (= 静的サイト前提を破る変更)

## スキルの活用

`.claude/skills/` に置いたスキルは、 該当する作業のときに自動でサジェスト/呼び出されます。
推奨スキル (= 別途インストール):

- **/loop** — 反復タスク (= バランス調整シミュレーション、 PR 監視)
- **/schedule** — 定刻実行 (= 日次レポート)
- **simplify** — 変更コードの DRY/品質チェック
- **fewer-permission-prompts** — 権限プロンプト削減
- **anthropic-skills:skill-creator** — 新スキル作成

## メモリの使い方

`~/.claude/memory/` に bearko ユーザー固有の記憶があります。
プロジェクト固有の情報は **CLAUDE.md と docs/** に書く (= メモリには書かない)。
理由: 複数プロジェクトで参照可能にするため。

## エラー対応の原則

- **silent fail を避ける** — 例外 catch は console.error 必須
- **ユーザーには簡潔に報告** — どこが失敗したか、 次の選択肢
- **修正前に診断** — root cause を特定してから fix
- **PR で発見した問題** — 別 issue or 別 PR で。 同 PR に詰め込まない

## エージェント間の連絡

`SendMessage` ツールで稼働中エージェントとやり取りできる。 用途:

- 並列起動した sibling agent に追加情報を渡す
- 長時間 sleep しているエージェントを起こす
